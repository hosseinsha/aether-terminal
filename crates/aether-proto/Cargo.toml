[package]
name = "aether-proto"
version = "0.1.0"
edition = "2021"
description = "Transport-agnostic protocol shared between the Aether client and session server."

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1.3"
tokio = { version = "1", features = ["io-util"] }
